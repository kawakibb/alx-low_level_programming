#include "main.h"

/**
 * print_most_numbers -  character is digit
 * Return: 1 if digit or 0 if not
 */
void print_most_numbers(void)
{
	char c;

	for (c = '0'; c <= '9'; c++)
	{
	if (c == '2' || c == '4')
	{

	}
	else
	{
		_putchar(c);
	}
	}
	_putchar('\n');

}
