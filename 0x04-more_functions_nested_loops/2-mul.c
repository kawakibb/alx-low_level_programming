#include "main.h"

/**
 * mul -  character is digit
 * @a: The value of first numbre
 * @b: The value of second number
 * Return: 1 if digit or 0 if not
 */

int mul(int a, int b)
{
	int x
	x = a * b;
	return (x);
}
