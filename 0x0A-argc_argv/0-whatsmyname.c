#include <stdio.h>
#include "main.h"

/**
 * main - writ the programs name
 * @argc: number of array arg
 * @argv: arguments
 * Return: 0 if Success
 */
int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
